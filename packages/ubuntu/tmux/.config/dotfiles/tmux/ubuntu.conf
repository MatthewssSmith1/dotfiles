# Replace the Omarchy-only help command without modifying the upstream snapshot.
unbind-key ?
bind-key -N "Show Tmux keybindings" ? display-popup -E -w 80% -h 70% -T "Tmux keybindings" "less -R \"$HOME/.config/dotfiles/tmux/keybindings.txt\""
